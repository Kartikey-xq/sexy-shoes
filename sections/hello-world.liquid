{%- for block in section.blocks -%}
    <h2>{{ block.type }}</h2>
{%- endfor -%}

{% schema %}
{
  "name": "Hello World Section",
  "class": "hello-world",
  "settings": [  ],
  "blocks": [
    {
        "type": "title",
        "name": "Title Block",
        "settings": [
            {
                "type": "color",
                 "id": "bg_color",
                "label": "Background Color",
                "default": "#e9e9e9",
    },
    {
        "type": "color",
        "id": "text_color",
        "label": "Text Color",
        "default": "#333333"
    }
]
    }
  ],
  "presets": [
    {
      "name": "Hello World",
        
    }
  ]
}
{% endschema %}